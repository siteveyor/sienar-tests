import NarrowContainer from '@/components/ui/NarrowContainer';

export default function NotFound() {
	return (
		<NarrowContainer>
			<h1>Not found</h1>
			<p>Sorry, we didn't find anything at that address.</p>
		</NarrowContainer>
	);
}